<UserControl
    x:Class="Reloaded.Mod.Launcher.Pages.Components.ModItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
    xmlns:local="clr-namespace:Reloaded.Mod.Launcher.Pages.Components"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Checkbox  -->
        <CheckBox
            Grid.RowSpan="2"
            Height="{DynamicResource ListEntryCheckboxHeightSmall}"
            Margin="4,0,8,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Focusable="False"
            IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            IsEnabled="{Binding IsEditable}"
            IsThreeState="False"
            Style="{DynamicResource DefaultCheckBox}" />

        <!--  Top Half  -->
        <TextBlock
            Grid.Column="1"
            MaxWidth="400"
            FontFamily="{DynamicResource FontBold}"
            FontSize="14"
            Text="{Binding Tuple.Config.ModDisplayName, UpdateSourceTrigger=PropertyChanged}"
            TextTrimming="CharacterEllipsis"
            ToolTip="{Binding Tuple.Config.ModDisplayName, UpdateSourceTrigger=PropertyChanged}" />

        <Separator
            Grid.Column="2"
            Margin="8,0,0,0"
            VerticalAlignment="Center"
            Opacity="0.15" />

        <StackPanel Grid.Column="3" Orientation="Horizontal">
            <StackPanel.Resources>
                <Style x:Key="StatusIcon" TargetType="{x:Type fa:IconBlock}">
                    <Setter Property="Width" Value="16" />
                    <Setter Property="Margin" Value="8,0,0,0" />
                    <Setter Property="Opacity" Value="0.20" />
                </Style>
            </StackPanel.Resources>

            <fa:IconBlock
                Icon="Heart"
                IconFont="Solid"
                ToolTip="Support the creator!">
                <fa:IconBlock.Style>
                    <Style BasedOn="{StaticResource StatusIcon}" TargetType="{x:Type fa:IconBlock}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsSupportable}" Value="True">
                                <Setter Property="Opacity" Value="1" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </fa:IconBlock.Style>
            </fa:IconBlock>

            <fa:IconBlock Icon="Gear">
                <fa:IconBlock.Style>
                    <Style BasedOn="{StaticResource StatusIcon}" TargetType="{x:Type fa:IconBlock}">
                        <Setter Property="ToolTip" Value="No config available." />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsConfigurable}" Value="True">
                                <Setter Property="Opacity" Value="1" />
                                <Setter Property="ToolTip" Value="Mod config available." />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </fa:IconBlock.Style>
            </fa:IconBlock>

            <fa:IconBlock>
                <fa:IconBlock.Style>
                    <Style BasedOn="{StaticResource StatusIcon}" TargetType="{x:Type fa:IconBlock}">
                        <Setter Property="Icon" Value="Rotate" />
                        <Setter Property="ToolTip" Value="No update support." />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsUpdatable}" Value="True">
                                <Setter Property="Opacity" Value="1" />
                                <Setter Property="ToolTip" Value="Check for updates?" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding HasNewUpdate}" Value="True">
                                <Setter Property="Icon" Value="Download" />
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect
                                            BlurRadius="8"
                                            Opacity="0.75"
                                            RenderingBias="Performance"
                                            ShadowDepth="0"
                                            Color="{DynamicResource AccentColorHighlight}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="{DynamicResource AccentColorHighlightBrush}" />
                                <Setter Property="ToolTip" Value="Download new update!" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </fa:IconBlock.Style>
            </fa:IconBlock>
        </StackPanel>

        <!--  Bottom Half  -->
        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Grid.ColumnSpan="3"
            Opacity="0.9"
            Orientation="Horizontal">

            <TextBlock
                Grid.Column="1"
                FontSize="12"
                Text="{Binding Tuple.Config.ModVersion, UpdateSourceTrigger=PropertyChanged}">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasNewUpdate}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource AccentColorHighlightBrush}" />
                                <Setter Property="FontFamily" Value="{DynamicResource FontBold}" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock
                Grid.Column="1"
                Margin="8,0"
                FontSize="12"
                Text="|" />

            <TextBlock FontSize="12" Text="{Binding Tuple.Config.ModAuthor, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</UserControl>
